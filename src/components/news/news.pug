mixin news(props)
    //- - let desc = props.desc.join(' ').slice(0, 27)
    .news&attributes(attributes)
        .news__img-wrapper 
            img.news__img(src= attributes.src)
            .news__link-wrapper 
                a(href="#", target="_blank") 
                    svg.news__link #[use(xlink:href='icons.svg#fb')]
                a(href="#", target="_blank") 
                    svg.news__link #[use(xlink:href='icons.svg#insta')]
            .news__caption
                p Автор: #[strong #{props.author}] 
                p Фото: #[strong #{props.photographer}] 
        .news__title= props.title
        .news__desc.text= props.shortDesc
        a.news__button.button.js-more-show(href= attributes.href) Подробнее