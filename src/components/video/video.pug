mixin video(props)
    .video-container
        //-mobile cover
        block 
        .video-wrapper&attributes(attributes)
            button.video-close.js-video-close(onclick= 'closeFullscreen()')
                svg(width="26" height="26" viewBox="0 0 26 26" fill="none" xmlns="http://www.w3.org/2000/svg")
                    path(d="M25 1L1 25M1 1L25 25" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round")

            video.video(muted="", preload="none", data-autoplay, poster= `assets/${props.poster}`)
                source(src= `assets/${props.video}.mp4`, type="video/mp4")
                source(src= `assets/${props.video}.webm`, type="video/webm")
